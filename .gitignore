# Secrets — cPanel/SFTP credentials parsed from the hosting welcome email.
.env
.env.*
*-cpanel

# Local tooling
.deploy-venv/
node_modules/

# Build output — regenerated by `npm run site:build` (publish this, don't commit).
site-dist/

# Legal / business documents — NEVER commit or publish (Idaho registration,
# Partner Center verification paperwork, etc.). Keep these local only.
*-business-*.pdf
permylastwebhook-business-idaho.pdf
*-registration*.pdf
misc/

# OS cruft
.DS_Store
