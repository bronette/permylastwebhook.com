{
  "name": "permylastwebhook-site",
  "version": "1.0.0",
  "private": true,
  "description": "Marketing + setup site for Per My Last Webhook (permylastwebhook.com).",
  "scripts": {
    "site:build": "node scripts/gen-integration-pages.mjs && node scripts/gen-sitemap.mjs && node scripts/fill-site-placeholders.mjs && node scripts/validate-site.mjs",
    "site:validate": "node scripts/validate-site.mjs",
    "deploy": ".deploy-venv/bin/python scripts/deploy_site.py",
    "gen:integrations": "node scripts/gen-integration-pages.mjs",
    "gen:sitemap": "node scripts/gen-sitemap.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
